{% extends 'base.html.twig' %}

{% block title %}Stock disponible{% endblock %}

{% block body %}
    <h1>Stock disponible chez {{stock.company.name}}</h1>

    {{ include('stock/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('stock_index') }}">back to list</a>

    {{ include('stock/_delete_form.html.twig') }}
{% endblock %}
