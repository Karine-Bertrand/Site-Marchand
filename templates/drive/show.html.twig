{% extends 'base.html.twig' %}

{% block title %}Drive{% endblock %}

{% block body %}
    <h1>Drive</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drive.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ drive.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('drive_index') }}">back to list</a>

    <a href="{{ path('drive_edit', {'id': drive.id}) }}">edit</a>

    {{ include('drive/_delete_form.html.twig') }}
{% endblock %}
