{% extends 'base.html.twig' %}

{% block title %}Edit Detail{% endblock %}

{% block body %}
    <h1>Edit Detail</h1>

    {{ include('detail/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('detail_index') }}">back to list</a>

    {{ include('detail/_delete_form.html.twig') }}
{% endblock %}
